import 'package:flutter/material.dart';

class classpg extends StatelessWidget {
  const classpg({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Sumita"),
    );
  }
}